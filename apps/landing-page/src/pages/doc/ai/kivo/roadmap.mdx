---
layout: '../../../../layouts/DocLayout.astro'
title: "Kivo — Roadmap & Technical Decisions"
description: "Current build status, phased implementation plan, open technical decisions, and changelog for Kivo."
---

# Roadmap & Technical Decisions

This document tracks where Kivo is today, where it is going, the open architectural questions that are not yet resolved, and a changelog of significant decisions.

Kivo Gateway is the runtime. Kivo Studio is the AI-agent creation environment. Kivo SDK is the downloadable integration layer. Power Totem is the functional hackathon template. Other templates are roadmap or marketplace candidates.

---

## Hackathon

- Power Totem as the functional template.
- Kivo Gateway physical flow.
- x402, Stellar, and Etherfuse validation visible.
- Studio flow creation, validation runs, launch options, and SDK export path.
- Health/status and receipt surfaces.

---

## After Hackathon

- Kivo Studio as the AI-agent workspace for flows, config, SDK snippets, tests, and launch checklist.
- Complete downloadable TypeScript SDK.
- Digital gateway adapters.
- Private mainnet publication as a paid path.
- Public templates as fallback for users who do not pay for private mainnet usage.
- Additional templates: API Toll, Agent Tool Paywall, Compute Gate, Automation Gate, IoT Device Access, EV Charging, and P2P Energy.

---

## Current Status

| Area | Status | Notes |
|---|---|---|
| Landing page | Complete | Deployed on Cloudflare Pages |
| Documentation | Complete | This document set |
| Supabase Edge API | Active | `kivo-api` owns health, deploy checks, Studio, Power Totem, x402, Etherfuse, gateway, dashboard, webhooks, API keys, MCP, and workflows |
| Kivo Studio routes | Active | `POST /v1/studio/intents`, `POST /v1/studio/flows`, validation runs, launch options, and templates |
| Supabase schema | Active | `kivo_*` tables, RLS, grants, Power Totem tables, gateway events, and x402/session bridge alignment |
| Power Totem template | Active | Only functional hackathon template |
| Etherfuse anchor | Active | Sandbox/testnet state visible through Edge routes |
| x402 payment path | Active | Challenge and signed Stellar XDR validation routes available |
| Kivo SDK | In progress | Downloadable TypeScript integration layer with client, adapters, examples, and tests |
| Other templates | Future | Roadmap or marketplace candidates |
| Soroban integration | Future | Phase 4 |

---

## Phase 1 — Foundation (Now)

**Goal:** Supabase Edge Function runtime with Studio, Power Totem, x402, Etherfuse, gateway events, dashboard health, and SDK integration surfaces running on testnet.

**Deliverables:**

- Supabase Edge Function `kivo-api`
- Supabase schema migrations applied for `kivo_*` tables, RLS, grants, Power Totems, gateway events, payments, webhooks, API keys, and x402/session bridge state
- `POST /v1/studio/intents` and `POST /v1/studio/flows` for Studio-led creation
- `POST /v1/studio/flows/:flowId/validation-runs` for testnet validation
- `GET /v1/studio/flows/:flowId/launch-options` and `GET /v1/studio/templates` for publication and fallback templates
- `GET|POST /v1/power-totems`, `POST /v1/power-totems/:id/pairing-token`, and `GET|POST /v1/power-sessions`
- `POST /v1/power-sessions/:id/start-checkout`, `POST /v1/x402/pay`, and `GET /power-totem/{id}/session`
- Etherfuse sandbox/testnet anchor endpoints: status, onboarding URL, assets, quote, order, fiat received simulation, and signed webhook
- Supabase Auth JWT middleware
- `GET /v1/health`, `GET /v1/deploy/checks`, and `GET /v1/deploy/services`
- Testnet Friendbot funding helper for dev
- Vercel frontend connected to Supabase Edge API and secrets

**Timeline:** Active hackathon track

---

## Phase 2 — x402 + MCP

**Goal:** x402 middleware protecting real endpoints, MCP server exposing Kivo tools to AI agents.

**Deliverables:**

- `GET /api/x402/data` — example x402-protected endpoint
- x402 middleware: nonce issuance, replay prevention, Stellar XDR verification, async settlement
- MCP server: `kivo_create_payment`, `kivo_check_status`, `kivo_simulate_payment`
- Claude Desktop integration guide + test script
- Conditional payments: `energy_kwh` condition type with threshold check
- `POST /v1/payments/:id/condition-proof` — condition proof submission
- Webhook worker goroutine: Redis → HMAC-signed HTTP POST
- `POST /v1/webhooks` — webhook registration with HMAC secret
- Rate limiting middleware (Redis sliding window)
- Testnet end-to-end test: device registers, creates conditional payment, submits proof, confirms on Stellar

**Timeline:** 4–6 weeks

---

## Phase 3 — EV Charging Template

**Goal:** Production-ready EV charging integration showing a real device-to-device payment flow with physical hardware simulation.

**Deliverables:**

- OCPP (Open Charge Point Protocol) bridge: translate OCPP events to Kivo payment conditions
- EV charging demo: Node.js simulator for car + charger, Kivo SDK integration
- Real-time kWh tracking → condition fulfillment → Stellar payment
- Dashboard: live session view, payment history, charger map
- React dashboard MVP with device management, payment history, webhook config
- Cloudflare Pages deployment of dashboard
- Public testnet demo: `demo.kivo.pay`

**Timeline:** 6–8 weeks

---

## Phase 4 — Soroban Smart Contracts

**Goal:** Complex conditional payments using Soroban smart contracts instead of server-side condition logic.

**Deliverables:**

- Soroban contract for multi-party conditional escrow
- Go SDK for Soroban contract invocation (`txnbuild.InvokeHostFunction`)
- Oracle integration: signed readings from external data sources (energy meter, GPS, weather)
- Multi-step payment channels: streaming micropayments per kWh with Soroban state
- Cross-device payment routing: A pays B pays C (payment chain) in single atomic tx
- Soroban testnet deployment + integration tests

**Timeline:** 8–12 weeks

---

## Phase 5 — Energy Trading + AI Economy

**Goal:** P2P energy trading marketplace and AI agent economy tooling.

**Deliverables:**

- P2P Energy Marketplace: solar panel devices list excess energy, buyers bid in USDC
- Automated market maker: device-to-device order book on Stellar DEX
- AI Agent SDK: higher-level abstractions for agents (budget manager, approval thresholds)
- `kivo_execute_contract` and `kivo_subscribe_webhook` MCP tools complete
- Batch payments: one-to-many device payments in a single Stellar transaction
- Payment analytics API: device revenue, spend, session history
- Grafana dashboard template for device operators

**Timeline:** 3–6 months

---

## Open Technical Decisions

### 1. RabbitMQ vs Redis for Queue at Scale

**Current:** Redis (Upstash) with `LPUSH`/`BRPOP`.

**Problem at scale:** Redis lists provide no message acknowledgment. If a payment worker crashes after dequeuing but before submitting to Stellar, the payment is lost.

**Options:**
- **Redis Streams** (`XADD`/`XREADGROUP`): adds consumer groups and acknowledgment without a new dependency. Simple upgrade path.
- **RabbitMQ** (self-hosted or CloudAMQP): full message broker with dead letter queues, routing, and durable acknowledgment. Better for complex routing (payment events → webhook worker → retry queue).
- **NATS JetStream**: high-throughput, lightweight, great for M2M messaging. Slightly less ecosystem support in Go compared to RabbitMQ.

**Decision criteria:** Migrate when payment volume exceeds 5,000 payments/day or when a payment loss incident occurs in production.

**Current lean:** Redis Streams first (zero new dependencies), then RabbitMQ if routing complexity grows.

---

### 2. Rust Rewrite of Payment Hot Path

**Context:** The payment submission path (decrypt Stellar secret → build transaction → sign → submit to Horizon) is latency-sensitive. For EV charging or energy trading use cases, every millisecond of settlement delay has user-visible impact.

**Options:**
- **Stay in Go:** Already fast (sub-millisecond signing). Horizon network latency (200–500ms) dominates — Go vs Rust makes no difference.
- **Rust sidecar for signing:** The secret decryption and transaction signing runs in a Rust binary called via Unix socket. Go handles HTTP, Rust handles crypto. Reduces attack surface for secret handling.
- **Full Rust rewrite:** Axum + sqlx. Maximum performance, but significant rewrite cost.

**Current decision:** Stay in Go. Revisit at Phase 4 when Soroban contract invocation adds complexity.

---

### 3. Soroban vs Claimable Balances for Conditional Payments

**Current:** Claimable Balances for escrow-style conditional payments (lock funds, release on condition, refund on timeout).

**Problem:** Claimable Balances are simple but not programmable. The condition logic (energy_kwh threshold, oracle verification) runs on Kivo's server, not on-chain. This requires trust in Kivo.

**Soroban option:** Deploy a Soroban contract that holds the escrow, verifies the condition (via signed oracle data submitted to the contract), and releases funds trustlessly. No Kivo server involvement after payment creation.

**Trade-offs:**

| | Claimable Balances | Soroban |
|---|---|---|
| Trustless | No (Kivo verifies) | Yes (contract verifies) |
| Gas cost | ~0.00001 XLM | 0.01–0.10 XLM |
| Complexity | Low | High |
| Latency | 3–5s | 3–5s |
| Available now | Yes | Testnet only |

**Current decision:** Claimable Balances for Phase 1–3. Soroban for Phase 4+ where trustless verification is required (energy trading, AI agent economy).

---

### 4. Self-Hosted Supabase vs Managed at Scale

**Current:** Supabase managed (free tier → $25/month plan).

**Problem:** At 1 million+ registered devices, the Supabase managed plan becomes expensive (~$600+/month for the Pro plan with add-ons). Each device creates rows in `devices`, potentially multiple rows in `payments` per day.

**Options:**
- **Supabase Pro ($25/month):** 8 GB storage, 15 GB bandwidth — handles ~800k devices.
- **Supabase Team ($599/month):** For serious production scale.
- **Self-hosted Supabase on Fly.io:** Run `supabase/postgres` Docker image on Fly.io volumes. ~$20/month for 50 GB. Operational burden increases significantly (backups, HA, upgrades).
- **Neon Serverless Postgres:** Branching for dev/prod, autoscaling — different from Supabase; loses RLS integration.

**Current decision:** Managed Supabase through $25/month plan. Evaluate self-hosted at 500k+ devices.

---

## Changelog

### 2026-05-16 — Etherfuse Anchor Added
Documented the Etherfuse sandbox/testnet anchor integration for Kivo, including onboarding, rampable assets, quotes, orders, sandbox fiat simulation, webhook verification, deployment variables, and the Kivo backend contract.

### 2025-05-15 — Documentation Complete
All 11 documentation files written covering architecture, data models, API reference, auth, Stellar integration, Etherfuse integration, x402, MCP, deployment, and roadmap.

### 2025-04-28 — ADR-006: x402 Protocol Adopted
Decided to implement x402 as the machine payment protocol rather than a custom header scheme. Aligned with emerging open standard for HTTP micropayments.

### 2025-04-15 — ADR-005: Upstash Redis Selected
Selected Upstash Redis over self-hosted Redis for the job queue. Free tier sufficient for MVP. go-redis v9 client chosen for its context-aware API.

### 2025-04-01 — ADR-001–004: Core Stack Finalized
Go + Fiber, sqlc, pgx v5, and Supabase PostgreSQL selected as the core technology stack. Decision documented in ADRs to prevent future revisiting.

### 2025-03-20 — MCP Server Added to Scope
After reviewing the Anthropic MCP specification, the Kivo MCP server was added to the product roadmap as Layer 3. Tool specifications drafted for `kivo_create_payment`, `kivo_check_status`, `kivo_execute_contract`, `kivo_subscribe_webhook`, `kivo_simulate_payment`.

### 2025-03-01 — Project Initiated
Kivo initiated as the third product in the Stellar Global Rails platform, targeting M2M payments and the emerging AI Agent Economy.

---

## Contributing

Kivo is pre-launch. If you are building M2M payment infrastructure, AI agent tooling, or EV charging systems and want to integrate Kivo, reach out via the SGR platform or open a discussion on the GitHub repository.

Architecture proposals, alternative ADRs, and use case contributions are welcome. Open a GitHub Issue tagged `RFC` for substantive architectural changes.
