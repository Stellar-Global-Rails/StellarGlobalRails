---
layout: '../../../../layouts/DocLayout.astro'
title: "Kivo"
description: "Kivo is a physical and digital gateway for monetizing access to resources."
---

# Kivo

Kivo is a physical and digital gateway for monetizing access to resources.

It combines:

- Kivo Gateway for runtime enforcement near a device, API, automation, worker, proxy, sidecar, or agent tool;
- Kivo Studio for AI-agent-assisted solution building;
- Kivo SDK for TypeScript integration;
- x402 for paid access requirements;
- Stellar for settlement proof;
- Etherfuse for visible anchor/funding context;
- private mainnet billing for validated flows that must remain private.

The hackathon functional template is Power Totem. Other templates are roadmap accelerators.

---

## Product Shape

Kivo treats every paid thing as a protected resource. A resource can be physical, such as a Raspberry Pi, power totem, kiosk, edge box, or local controller. A resource can also be digital, such as an API, worker, automation, proxy, sidecar, queue consumer, or agent tool.

The core flow is:

```txt
resource -> policy -> x402 requirement -> Stellar proof -> Etherfuse context -> Kivo authorization -> Gateway release -> receipt and health
```

Kivo Gateway enforces the authorization. Kivo Studio helps users design the flow with AI agents. Kivo SDK gives developers the TypeScript integration surface for resources, policies, payment requirements, authorizations, gateway events, webhooks, and tests.

---

## Functional Template

Power Totem is the hackathon functional template.

It demonstrates a physical gateway flow:

1. A totem displays a QR checkout.
2. The user pays through an x402 requirement.
3. Kivo validates Stellar settlement proof.
4. Kivo shows Etherfuse anchor/funding context.
5. Kivo issues a short-lived authorization.
6. The physical gateway releases a safe load.
7. Kivo records status, health, receipt, and completion events.

Other templates, including API Toll, Data Gate, Agent Tool Paywall, Compute Gate, Automation Gate, IoT Device Access, EV Charging, and P2P Energy, are roadmap accelerators rather than hackathon-ready features.

---

## Templates

<a id="template-power-totem"></a>

### Template Power Totem

Power Totem is the first functional Kivo template. It represents a physical gateway that releases a safe load only after Kivo validates the x402 payment requirement, settlement proof, Etherfuse funding context, and short-lived authorization.

<a id="template-api-toll"></a>

### Template API Toll

API Toll applies the same resource-policy-authorization model to an HTTP route. A protected endpoint returns an x402 requirement, Kivo verifies payment state, and the digital gateway lets the request continue only while the authorization is valid.

<a id="template-agent-tool-paywall"></a>

### Template Agent Tool Paywall

Agent Tool Paywall adapts Kivo for AI tool calls. An agent requests a paid tool, satisfies the x402 requirement, and receives a scoped authorization for that tool invocation without relying on a standing API key or monthly billing account.

---

## Next Steps

- [Architecture](/doc/ai/kivo/architecture) - how the gateway model is organized
- [API Reference](/doc/ai/kivo/api-reference) - resource, policy, authorization, and gateway event APIs
- [Etherfuse Integration](/doc/ai/kivo/etherfuse-integration) - visible anchor/funding context
- [x402 Protocol](/doc/ai/kivo/x402-protocol) - paid access requirement layer
- [Roadmap](/doc/ai/kivo/roadmap) - hackathon scope and post-hackathon plan
